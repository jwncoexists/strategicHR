<fieldset>
  <%= f.select(:video_id, options_from_collection_for_select(Video.all, :id, :name)) %>
  <%= f.select(:quiz_id, options_from_collection_for_select(Quiz.all, :id, :name)) %>
  <%= f.hidden_field :_destroy %>
  <%= link_to "remove", '#', class: "remove_fields" %>
</fieldset>
<br>